<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="poc-deploying-quay-cli">
  <title>Deploy the Red Hat Quay registry by using the CLI</title>
  <shortdesc>To deploy your proof of concept Red Hat Quay registry, you can start the registry container with Podman.</shortdesc>
  <taskbody>
    <steps-unordered>
      <step>
        <cmd>Enter the following command to start the <codeph>Quay</codeph> registry container, specifying the appropriate volumes for configuration data and local storage for image data:
</cmd>
        <info>
          <codeblock>$ sudo podman run -d --rm -p 80:8080 -p 443:8443  \
   --name=quay \
   -v $QUAY/config:/conf/stack:Z \
   -v $QUAY/storage:/datastorage:Z \
   registry.redhat.io/quay/quay-rhel9:v3.18.0</codeblock>
        </info>
      </step>
    </steps-unordered>
  </taskbody>
</task>
