// module included in the following assemblies:

// * use_quay/master.adoc
// * quay_io/master.adoc

:_mod-docs-content-type: PROCEDURE
[id="deleting-team-within-organization-api"]
= Deleting a team within an organization by using the API

[role="_abstract"]
To delete a team from an organization in {productname}, you can use the organization team API endpoint with an OAuth access token.

.Prerequisites

* You have created an OAuth access token.

.Procedure

* You can delete a team within an organization by entering the `DELETE /api/v1/organization/{orgname}/team/{teamname}` command:
+
[source,terminal]
----
$ curl -X DELETE \
  -H "Authorization: Bearer <your_access_token>" \
  "<quay-server.example.com>/api/v1/organization/<organization_name>/team/<team_name>"
----
+
This command does not return output in the CLI.
