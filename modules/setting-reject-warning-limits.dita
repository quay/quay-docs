<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="setting-reject-warning-limits">
  <title>Setting reject and warning limits by using the API</title>
  <shortdesc>To configure reject and warning thresholds for an organization quota in Red Hat Quay, you can post limit definitions to the organization quota limit API endpoint.</shortdesc>
  <taskbody>
    <steps>
      <step>
        <cmd>To set a reject limit, send a <codeph>POST</codeph> request to the <codeph>/api/v1/organization/&lt;organization_name&gt;/quota/&lt;quota_id&gt;/limit</codeph> endpoint. For example:
</cmd>
        <info>
          <codeblock outputclass="language-terminal">$ curl -k -X POST -H "Authorization: Bearer &lt;token&gt;" -H 'Content-Type: application/json' -d '{"type":"Reject","threshold_percent":80}'  https://&lt;quay-server.example.com&gt;/api/v1/organization/&lt;organization_name&gt;/quota/1/limit</codeblock>
          <ul>
            <li>To set a warning limit, send a <codeph>POST</codeph> request to the same endpoint. For example:
<codeblock outputclass="language-terminal">$ curl -k -X POST -H "Authorization: Bearer &lt;token&gt;" -H 'Content-Type: application/json' -d '{"type":"Warning","threshold_percent":50}'  https://&lt;quay-server.example.com&gt;/api/v1/organization/&lt;organization_name&gt;/quota/1/limit</codeblock></li>
          </ul>
        </info>
      </step>
    </steps>
  </taskbody>
</task>
