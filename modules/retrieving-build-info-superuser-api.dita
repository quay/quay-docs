<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="retrieving-build-info-superuser-api">
  <title>Retrieving build information with the Red Hat Quay API</title>
  <shortdesc>To retrieve build details, status, and logs as a Red Hat Quay superuser, you can use the superuser build API endpoints.</shortdesc>
  <taskbody>
    <prereq>
      <ul>
        <li>You have created an OAuth access token.</li>
        <li>You have superuser privileges.</li>
      </ul>
    </prereq>
    <steps>
      <step>
        <cmd>Enter the following command to return information about a build by using the <codeph>GET /api/v1/superuser/{build_uuid}/build</codeph> endpoint:
</cmd>
        <info>
          <codeblock outputclass="language-terminal">$ curl -X GET "https://quay-server.example.com/api/v1/superuser/&lt;build_uuid&gt;/build" \
  -H "Authorization: Bearer &lt;ACCESS_TOKEN&gt;"</codeblock>
        </info>
      </step>
      <step>
        <cmd>Enter the following command to return the status for builds that you specify by UUID by using the <codeph>GET /api/v1/superuser/{build_uuid}/status</codeph> endpoint:
</cmd>
        <info>
          <codeblock outputclass="language-terminal">$ curl -X GET "https://quay-server.example.com/api/v1/superuser/&lt;build_uuid&gt;/status" \
  -H "Authorization: Bearer &lt;ACCESS_TOKEN&gt;"</codeblock>
        </info>
      </step>
      <step>
        <cmd>Enter the following command to return the build logs for a build that you specify by UUID by using the <codeph>GET /api/v1/superuser/{build_uuid}/logs</codeph> endpoint:
</cmd>
        <info>
          <codeblock outputclass="language-terminal">$ curl -X GET "https://quay-server.example.com/api/v1/superuser/&lt;build_uuid&gt;/logs" \
  -H "Authorization: Bearer &lt;ACCESS_TOKEN&gt;"</codeblock>
        </info>
      </step>
    </steps>
  </taskbody>
</task>
