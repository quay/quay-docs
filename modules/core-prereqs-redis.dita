<?xml version="1.0"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="core-prereqs-redis">
  <title>Redis backend</title>
  <shortdesc>Red Hat Quay uses Redis to support backend tasks such as live builder logs, user events, and the locking mechanism that is required for garbage collection. Redis data is ephemeral.</shortdesc>
  <conbody>
    <p>You do not need high availability for Redis because the data is not persistent. If Redis fails, you will lose access to build logs, builders, and the garbage collector service. Additionally, user events will be unavailable.</p>
    <p>For geo-replication, a single Redis cache is shared across the entire Red Hat Quay setup and must be accessible by all Red Hat Quay pods.</p>
    <p>You can use a Redis image from the Red Hat Software Collections or from any other source you prefer.</p>
  </conbody>
</concept>
