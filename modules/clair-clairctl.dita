<?xml version="1.0"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="clair-disconnected-ocp-configuration">
  <title>Setting up Clair in a disconnected OpenShift Container Platform cluster</title>
  <shortdesc>To install the <codeph>clairctl</codeph> command-line utility on a disconnected OpenShift Container Platform cluster, you can copy the binary from a running Clair pod and make it executable.</shortdesc>
  <taskbody>
    <steps>
      <step>
        <cmd>Install the <codeph>clairctl</codeph> program for a Clair deployment in an OpenShift Container Platform cluster by entering the following command:
</cmd>
        <info>
          <codeblock outputclass="language-terminal">$ oc -n quay-enterprise exec example-registry-clair-app-64dd48f866-6ptgw -- cat /usr/bin/clairctl &gt; clairctl</codeblock>
        </info>
      </step>
      <step>
        <cmd>Set the permissions of the <codeph>clairctl</codeph> file so that it can be executed and run by the user, for example:
</cmd>
        <info>
          <codeblock outputclass="language-terminal">$ chmod u+x ./clairctl</codeblock>
        </info>
      </step>
    </steps>
  </taskbody>
</task>
