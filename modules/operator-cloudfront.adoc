:_mod-docs-content-type: PROCEDURE
[id="operator-cloudfront"]
= AWS S3 CloudFront

[role="_abstract"]
To configure AWS S3 CloudFront for your {productname} backend registry storage, you can create a secret that includes your `config.yaml` file and the CloudFront signing key. This enables CloudFront content delivery for your registry storage.

.Procedure

* Create a secret that includes your `config.yaml` file and the CloudFront signing key by entering the following command:
+
[source,terminal]
----
$ oc create secret generic --from-file config.yaml=./config_awss3cloudfront.yaml --from-file default-cloudfront-signing-key.pem=./default-cloudfront-signing-key.pem test-config-bundle
----