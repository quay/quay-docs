:_mod-docs-content-type: CONCEPT
[id="setting-up-prometheus-to-consume-metrics"]
= Setting up Prometheus to consume metrics

[role="_abstract"]
To allow Prometheus to scrape metrics from every {productname} instance in a cluster, you can publish the instances under a single DNS name that Prometheus can resolve.
