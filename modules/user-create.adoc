
// module included in the following assemblies:

// * use_quay/master.adoc
// * quay_io/master.adoc

// Needs updated when v2 UI panel is available

:_mod-docs-content-type: CONCEPT
[id="user-create"]
= User accounts overview


[role="_abstract"]
A user account is an authenticated identity on {productname} that can create repositories, push and pull images, and manage access. You can create and delete users in the UI or by using the API.